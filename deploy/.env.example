# Separate AgentAgenda key registered in AIlauncher's APP_KEYS_FILE.
# Copy to .env on the server, protect with chmod 600. Never use the admin key.
LLM_API_KEY=

# Independent backend pairing/signing secret.
SECRET_KEY=
